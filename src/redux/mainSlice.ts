import { createSlice, PayloadAction } from "@reduxjs/toolkit";
// import { ToDoObj } from "../types"

// const initialState:ToDoObj[]=[]

// const toDoSlice = createSlice({
//     name:"ToDo's",
//     initialState,
//     reducers:{
//         addSth(state, action:PayloadAction<string>){//text:string
//             state.push({id:Date.now(),text:action.payload})
//         },
//         removeSth(state, action:PayloadAction<number>){
//         }
//     }
// })

// export const {addSth, removeSth} = .actions
// export const todosReducers = .reducer